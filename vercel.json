{
    "experimentalServices": {
        "frontend": {
            "framework": "create-react-app",
            "root": "frontend",
            "routePrefix": "/",
            "installCommand": "npm install --legacy-peer-deps --no-audit --no-fund && npm install ajv@^8 --legacy-peer-deps --no-audit"
        },
        "backend": {
            "root": "backend",
            "routePrefix": "/_/backend",
            "entrypoint": "server.py"
        }
    }
}
